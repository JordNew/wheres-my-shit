import { elements } from '../views/base';

export default class Heading2 {
    constructor() {

    }

    // updateNumItems(arr) {
    //     elements.headingByMe.innerHTML = `Borrowed ${arr === borrowedByMe? by : from} me (${JSON.parse(localStorage.arr).length}):`;    
    // }
    
}


